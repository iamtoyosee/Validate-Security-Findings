# Todo List

A small web-based todo list built with Python's standard library and SQLite.

Run it with:

```sh
python3 app.py
```

Then open `http://127.0.0.1:8000` in a browser.

The database is created automatically in the current directory.
